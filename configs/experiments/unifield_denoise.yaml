# Denoising experiment: field-aware Gaussian noise removal.
defaults:
  - ../default

data:
  task: denoise
  noise_sigma: 0.05
  patch_size: [96, 96, 48]

training:
  epochs: 150
  batch_size: 8
  lr: 2.0e-4

loss:
  l1_weight: 1.0
  perceptual_weight: 0.05
  ssim_weight: 0.2
