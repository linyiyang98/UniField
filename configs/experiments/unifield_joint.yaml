# Joint super-resolution + denoising experiment.
defaults:
  - ../default

data:
  task: sr+denoise
  scale_factor: 4
  noise_sigma: 0.05
  patch_size: [64, 64, 32]

training:
  epochs: 250
  batch_size: 4
  lr: 1.0e-4

loss:
  l1_weight: 1.0
  perceptual_weight: 0.1
  ssim_weight: 0.15
